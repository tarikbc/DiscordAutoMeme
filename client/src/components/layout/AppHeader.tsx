import NotificationCenter from '../common/NotificationCenter';

// Add the NotificationCenter component to the header
<div className="flex items-center space-x-4">
  <NotificationCenter />
  {/* Any existing header elements like user profile, etc. */}
</div> 